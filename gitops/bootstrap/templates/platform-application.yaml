apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: demo-java-app-platform
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  project: devops-lab
  source:
    repoURL: {{ required "repoUrl is required" .Values.repoUrl | quote }}
    targetRevision: {{ .Values.targetRevision | quote }}
    path: infra/external-secrets/demo-java-app
  destination:
    server: https://kubernetes.default.svc
    namespace: demo-dev
  syncPolicy:
    {{- if .Values.syncPolicy.automated }}
    automated:
      prune: {{ .Values.syncPolicy.prune }}
      selfHeal: {{ .Values.syncPolicy.selfHeal }}
    {{- end }}
    syncOptions:
      - CreateNamespace=true
