apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: payroll-database
  namespace: payroll-dev
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  refreshInterval: 1m
  secretStoreRef:
    kind: SecretStore
    name: openbao
  target:
    name: payroll-database
    creationPolicy: Owner
    deletionPolicy: Retain
  data:
    - secretKey: DB_HOST
      remoteRef:
        key: payroll/dev/database
        property: host
    - secretKey: DB_PORT
      remoteRef:
        key: payroll/dev/database
        property: port
    - secretKey: DB_USER
      remoteRef:
        key: payroll/dev/database
        property: username
    - secretKey: DB_PASSWORD
      remoteRef:
        key: payroll/dev/database
        property: password
